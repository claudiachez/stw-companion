# STW App (subscriber web app) — web-staging / web-main branches
[build]
  command = "cd web && npm install && npm run build"
  publish = "web/dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
