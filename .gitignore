.DS_Store
data/
node_modules/
*.log
dist/
apps/web/dist/
apps/admin/dist/
.env
**/.env
.env.local
**/.env.local

# IBKR proxy local TLS material (never commit)
*.pem

# Python build artifacts (ibkr_proxy.py)
__pycache__/
*.pyc

# Local DB snapshots (contain subscriber PII — never commit)
backups/

# Webapp redesign design references (large HTML/PNG — do not ship, keep local)
plans/20260720_webapp_redesign/refs/
